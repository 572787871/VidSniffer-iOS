# Local FFmpeg Binary

This directory is kept for a future native HLS/TS conversion path:

```text
ios/FFmpeg/bin/ffmpeg
```

The current native app does not bundle or execute this binary. It focuses on parsing,
browser sniffing, playback, and saving direct media resources. Replace the placeholder
with a real iOS-compatible FFmpeg build before wiring in local m3u8/ts conversion.
